const {compareAsc, format} = require('date-fns');
const mongoose = require('mongoose');
const Schema = mongoose.Schema;

//????? maybe i should move this to the thoughts schema since it not a model?????